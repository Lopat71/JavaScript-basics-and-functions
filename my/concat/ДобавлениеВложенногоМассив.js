// 7. Добавление вложенного массива:
// У вас есть массив array1 = [1, 2] и вложенный массив nestedArray = [ [3, 4] ]. Используйте метод concat(), чтобы добавить вложенный массив. Как будет выглядеть результат?

// Добавьте вложенный массив
let array1 = [1, 2];
let nestedArray = [[3, 4]];
let result = array1.concat(nestedArray);
console.log(result);
