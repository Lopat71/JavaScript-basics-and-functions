// Проверка пустого массива: Создайте пустой массив emptyArray = []; и попробуйте извлечь элементы с помощью slice(0, 1). Какой будет результат?

// let emptyArray = [];
// // Извлеките элементы с помощью slice(0, 1)

let emptyArray = [];

let newArray = emptyArray.slice(0, 1);
console.log(newArray);
