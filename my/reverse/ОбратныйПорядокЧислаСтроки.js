// 6. Обратный порядок с числами и строками:
// У вас есть массив mixed = [1, 'two', 3, 'four']. Используйте метод reverse(), чтобы изменить порядок элементов. Как будет выглядеть новый массив?

// Измените порядок элементов с помощью reverse()
let mixed = [1, "two", 3, "four"];
console.log(mixed.reverse());
