// 8. Обратный порядок вложенного массива:
// У вас есть массив nested = [[1, 2], [3, 4], [5, 6]]. Измените порядок вложенных массивов с помощью reverse(). Как будет выглядеть новый массив?

// Измените порядок вложенных массивов с помощью reverse()
let nested = [
    [1, 2],
    [3, 4],
    [5, 6],
];
console.log(nested.reverse());
