// 2. Сортировка чисел по убыванию:
// У вас есть массив numbers = [8, 1, 42, 23, 16]. Используйте метод sort(), чтобы отсортировать его по убыванию. Как будет выглядеть результат?

// Отсортируйте массив по убыванию
let numbers = [8, 1, 42, 23, 16];
console.log(numbers.sort((a, b) => b - a));
