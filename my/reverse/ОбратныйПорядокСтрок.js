// 2. Обратный порядок строк:
// У вас есть массив строк words = ['hello', 'world', 'JavaScript']. Используйте метод reverse() для изменения порядка строк. Как будет выглядеть новый массив?

// Измените порядок строк с помощью reverse()
let words = ["hello", "world", "JavaScript"];
console.log(words.reverse());
