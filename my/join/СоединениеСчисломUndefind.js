// Соединение массива с числом и undefined: У вас есть массив mixed = [10, undefined, 30]. Используйте join('-') для соединения элементов. Какой будет результат?

// Используйте дефис как разделитель
let mixed = [10, undefined, 30];

let newArray = mixed.join("-");
console.log(newArray);
