// Копирование массива с помощью splice(): У вас есть массив original = [1, 2, 3, 4, 5]. Используйте splice() для создания нового массива, который будет содержать все элементы. Как будет выглядеть новый массив?

// Создайте новый массив с помощью splice()
let original = [1, 2, 3, 4, 5];

let newArray = original.splice(0);
console.log(newArray);
