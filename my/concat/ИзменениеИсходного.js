// 9. Изменение исходного массива:
// Проверьте, изменится ли исходный массив после использования метода concat(). У вас есть массив original = [10, 20]. С помощью concat() создайте новый массив с элементами [30, 40] и проверьте, изменился ли массив original.

// Создайте новый массив с concat(), добавив [30, 40], и проверьте оригинальный массив
let original = [10, 20];
let result = original.concat([30, 40]);
console.log(result);
