// Извлечение подмассива до конца: У вас есть массив colors = ['red', 'green', 'blue', 'yellow']. Извлеките все элементы начиная с индекса 2 до конца массива. Какой будет новый массив?

// let colors = ['red', 'green', 'blue', 'yellow'];
// // Извлеките элементы начиная с индекса 2

let colors = ["red", "green", "blue", "yellow"];

let sliced = colors.slice(2);
console.log(sliced);
