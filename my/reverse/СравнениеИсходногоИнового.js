// 7. Сравнение исходного и изменённого массива:
// У вас есть массив data = [10, 20, 30]. После применения метода reverse() выведите как исходный, так и изменённый массив. Изменится ли исходный массив?

// Примените reverse() и сравните исходный и изменённый массив
let data = [10, 20, 30];
let newArray = data.reverse();
console.log(data);
console.log(newArray);
