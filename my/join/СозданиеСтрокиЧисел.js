// Создание строки чисел: У вас есть массив чисел numbers = [1, 2, 3, 4, 5]. Используйте join(), чтобы соединить все числа в одну строку без разделителя. Каким будет результат?

// Соедините числа в строку без разделителя
let numbers = [1, 2, 3, 4, 5];

let newArray = numbers.join();
console.log(newArray);
