// 5. Копирование и обратный порядок:
// У вас есть массив original = [1, 2, 3, 4, 5]. Создайте новый массив reversed с обратным порядком, не изменяя исходный массив. Как это сделать?

// Создайте новый массив reversed с обратным порядком
let original = [1, 2, 3, 4, 5];
let newArray = original.reverse();
console.log(newArray);
