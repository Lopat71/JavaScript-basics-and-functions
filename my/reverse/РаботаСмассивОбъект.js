// 4. Работа с массивом объектов:
// У вас есть массив объектов users = [{name: 'Alice'}, {name: 'Bob'}, {name: 'Charlie'}]. Используйте метод reverse(), чтобы изменить порядок объектов в массиве. Как будет выглядеть результат?

// Измените порядок объектов с помощью reverse()
let users = [{ name: "Alice" }, { name: "Bob" }, { name: "Charlie" }];
console.log(users.reverse());
