// 6. Добавление числа и массива:
// У вас есть массив array = [1, 2]. Добавьте число 3 и массив [4, 5] с помощью метода concat(). Как будет выглядеть новый массив?

// Добавьте число 3 и массив [4, 5] с помощью concat()
let array = [1, 2];
let result = array.concat(3, [4, 5]);
console.log(result);
