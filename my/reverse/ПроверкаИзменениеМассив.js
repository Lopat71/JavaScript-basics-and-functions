// 9. Проверка изменения массивов:
// Создайте два массива: firstArray = [7, 8, 9] и secondArray = firstArray.reverse(). Выведите оба массива и проверьте, изменился ли первый массив.

// Используйте reverse() и проверьте оба массива
let firstArray = [7, 8, 9];
let secondArray = firstArray.reverse();
console.log(firstArray);
console.log(secondArray);
