// Создание CSV строки: У вас есть массив data = ['name', 'age', 'city']. Объедините элементы с помощью разделителя , (запятая) для создания строки CSV. Как будет выглядеть итоговая строка?

// Используйте запятую для создания строки CSV
let data = ["name", "age", "city"];

let newArray = data.join();
console.log(newArray);
