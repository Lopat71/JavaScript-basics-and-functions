// Соединение массива с null: У вас есть массив values = ['a', null, 'c']. Используйте join('*') для соединения элементов. Какой будет результат?

// Используйте звездочку как разделитель
let values = ["a", null, "c"];

let newArray = values.join("*");
console.log(newArray);

