// Использование пользовательского разделителя: У вас есть массив colors = ['red', 'green', 'blue']. Объедините элементы массива с использованием разделителя " | ". Каким будет результат?

// Используйте разделитель " | "
let colors = ["red", "green", "blue"];

let newArray = colors.join("|");
console.log(newArray);
